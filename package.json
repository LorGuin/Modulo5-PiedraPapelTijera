{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base /piedra-papel-tijera",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -r git@github.com:LorGuin/piedra-papel-tijera.git -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.0"
  }
}
